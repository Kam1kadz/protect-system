<UserControl x:Class="KMGuardLoader.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- User info -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="{Binding User.Username}"
                           FontSize="20" FontWeight="Bold" Foreground="#ffffff"/>
                <TextBlock Text="{Binding User.PlanDisplayName}"
                           FontSize="12" Foreground="#7c3aed" Margin="0,2,0,0"/>
            </StackPanel>
            <Button Content="⚙" Margin="16,0,0,0"
                    Style="{StaticResource IconBtn}"
                    Command="{Binding Path=DataContext.SettingsCommand,
                              RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Content="Logout" Margin="8,0,0,0"
                    Style="{StaticResource SecondaryBtn}"
                    Command="{Binding LogoutCommand}"/>
        </StackPanel>

        <!-- Status area -->
        <Border Grid.Row="1" Background="#18181b" CornerRadius="8" Padding="16">
            <TextBlock Text="{Binding Status}" Foreground="#a1a1aa"
                       FontSize="13" VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Launch button -->
        <Button Grid.Row="2" Content="LAUNCH" Margin="0,20,0,0"
                Height="52" FontSize="16" FontWeight="Bold"
                Style="{StaticResource PrimaryBtn}"
                Command="{Binding LaunchCommand}"/>
    </Grid>
</UserControl>