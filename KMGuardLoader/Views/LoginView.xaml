<UserControl x:Class="KMGuardLoader.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid>
        <StackPanel Width="320" VerticalAlignment="Center" HorizontalAlignment="Center">

            <!-- Logo placeholder -->
            <TextBlock Text="KMGuard" FontSize="28" FontWeight="Bold"
                       Foreground="#7c3aed" HorizontalAlignment="Center" Margin="0,0,0,32"/>

            <TextBox x:Name="EmailBox" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource InputBox}" Margin="0,0,0,12"/>

            <PasswordBox x:Name="PassBox" Style="{StaticResource InputBox}"
                         Margin="0,0,0,20"
                         PasswordChanged="PassBox_Changed"/>

            <Button Content="LOG IN" Command="{Binding LoginCommand}"
                    Style="{StaticResource PrimaryBtn}" Height="42"/>

            <TextBlock Text="{Binding Status}" Foreground="#ef4444"
                       HorizontalAlignment="Center" Margin="0,12,0,0"
                       FontSize="12" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>