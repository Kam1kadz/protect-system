<Window x:Class="KMGuardLoader.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:KMGuardLoader.ViewModels"
        xmlns:views="clr-namespace:KMGuardLoader.Views"
        Title="{x:Static Member=local:TenantConfig.LoaderName}"
        xmlns:local="clr-namespace:KMGuardLoader"
        Width="820" Height="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="#0d0d0f"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
    </Window.Resources>

    <Border CornerRadius="12" BorderBrush="#2a2a2e" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Titlebar -->
            <Grid Grid.Row="0" Background="#18181b" MouseLeftButtonDown="Titlebar_Drag">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{x:Static Member=local:TenantConfig.LoaderName}"
                           Foreground="#ffffff" FontSize="12" FontWeight="SemiBold"
                           VerticalAlignment="Center" Margin="14,0,0,0"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,8,0">
                    <Button Content="—" Width="28" Height="28"
                            Style="{StaticResource TitleBarBtn}"
                            Click="Minimize_Click"/>
                    <Button Content="✕" Width="28" Height="28"
                            Style="{StaticResource TitleBarBtn}"
                            Click="Close_Click"/>
                </StackPanel>
            </Grid>

            <!-- Content -->
            <ContentPresenter Grid.Row="1"
                              Content="{Binding CurrentView}"/>
        </Grid>
    </Border>
</Window>